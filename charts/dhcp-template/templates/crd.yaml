apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: dhcptemplates.k8s.lukasdietrich.com
spec:
  group: k8s.lukasdietrich.com
  names:
    categories: []
    kind: DHCPTemplate
    plural: dhcptemplates
    shortNames: []
    singular: dhcptemplate
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[0].type
      name: Ready
      type: string
    - jsonPath: .status.conditions[0].message
      name: Message
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for DHCPTemplateSpec via `CustomResource`
        properties:
          spec:
            properties:
              template:
                type: string
            required:
            - template
            type: object
          status:
            nullable: true
            properties:
              conditions:
                items:
                  properties:
                    lastTransitionTime:
                      description: Time is a wrapper around time.Time which supports correct marshaling to YAML and JSON.  Wrappers are provided for many of the factory methods that the time package offers.
                      format: date-time
                      type: string
                    message:
                      type: string
                    observedGeneration:
                      format: int64
                      nullable: true
                      type: integer
                    reason:
                      enum:
                      - Reconciliation
                      - TemplateEvaluation
                      - PlanningObjects
                      - AllObjectsReady
                      - Unknown
                      type: string
                    status:
                      enum:
                      - 'True'
                      - 'False'
                      - Unknown
                      type: string
                    type:
                      enum:
                      - Pending
                      - Ready
                      - Error
                      - Unknown
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              objects:
                items:
                  properties:
                    apiVersion:
                      type: string
                    kind:
                      type: string
                    name:
                      type: string
                    namespace:
                      nullable: true
                      type: string
                  required:
                  - apiVersion
                  - kind
                  - name
                  type: object
                type: array
            required:
            - conditions
            - objects
            type: object
        required:
        - spec
        title: DHCPTemplate
        type: object
    served: true
    storage: true
    subresources:
      status: {}

